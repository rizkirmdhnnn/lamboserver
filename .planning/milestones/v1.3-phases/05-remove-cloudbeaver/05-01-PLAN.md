---
phase: 05-remove-cloudbeaver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.go
  - internal/services/service.go
  - internal/config/defaults.go
  - internal/services/cloudbeaver/cloudbeaver.go
  - internal/services/cloudbeaver/config.go
  - internal/services/cloudbeaver/installer.go
  - internal/services/cloudbeaver/service_adapter.go
  - frontend/src/pages/PgDatabasePage.tsx
  - docs/DEVELOPMENT.md
autonomous: true
requirements:
  - CLEAN-01

must_haves:
  truths:
    - "A codebase-wide search for 'cloudbeaver' (case-insensitive) returns zero results in source files (Go, TSX, MD under docs/)"
    - "The Go project compiles successfully with no CloudBeaver imports or references"
    - "PgDatabasePage.tsx renders PostgreSQL controls only -- no CloudBeaver panel, state, or functions"
    - "DefaultCloudBeaverPort constant does not exist in config"
  artifacts:
    - path: "app.go"
      provides: "Composition root without CloudBeaver import, field, registration, or special-casing"
      contains: "no cloudbeaver references"
    - path: "internal/services/service.go"
      provides: "Service interface package doc without CloudBeaver in comment"
      contains: "no CloudBeaver in interface assignment list"
    - path: "internal/config/defaults.go"
      provides: "Port constants without DefaultCloudBeaverPort"
      contains: "no DefaultCloudBeaverPort"
    - path: "frontend/src/pages/PgDatabasePage.tsx"
      provides: "PostgreSQL page without CloudBeaver panel"
      contains: "no cbInstalled, cbRunning, cbLoading, cbError, handleCbAction"
  key_links:
    - from: "app.go"
      to: "internal/services/"
      via: "import statements"
      pattern: "no cloudbeaver import"
    - from: "frontend/src/pages/PgDatabasePage.tsx"
      to: "wailsjs/go/main/App"
      via: "Wails IPC imports"
      pattern: "no OpenWebAdmin or GetWebAdminURL imports if unused by remaining code"
---

<objective>
Remove all CloudBeaver code from the Go backend, frontend UI, config constants, and documentation.

Purpose: CloudBeaver has a broken installer and dead download URL. This phase cleans the codebase so Phase 6 can add pgweb on a clean slate.
Output: A codebase with zero "cloudbeaver" references in source files. The PostgreSQL page shows only PostgreSQL service controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-remove-cloudbeaver/05-CONTEXT.md
@.planning/phases/05-remove-cloudbeaver/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove CloudBeaver from Go backend</name>
  <files>
    app.go,
    internal/config/defaults.go,
    internal/services/service.go,
    internal/services/cloudbeaver/cloudbeaver.go,
    internal/services/cloudbeaver/config.go,
    internal/services/cloudbeaver/installer.go,
    internal/services/cloudbeaver/service_adapter.go
  </files>
  <read_first>
    app.go,
    internal/config/defaults.go,
    internal/services/service.go
  </read_first>
  <action>
1. Delete the entire `internal/services/cloudbeaver/` directory (4 files: cloudbeaver.go, config.go, installer.go, service_adapter.go).

2. Edit `app.go`:
   - Remove the import line: `"github.com/rizkirmdhnnn/lamboserver/internal/services/cloudbeaver"`
   - Remove the struct field from `App`: `CloudBeaver  *cloudbeaver.Manager`
   - In `NewApp()`, remove: `cloudbeaverMgr := cloudbeaver.NewManager(paths)` (line 75)
   - In `NewApp()`, remove: `mgr.Register("cloudbeaver", cloudbeaver.NewServiceAdapter(cloudbeaverMgr))` (line 86)
   - In the `return &App{...}` block, remove: `CloudBeaver:  cloudbeaverMgr,` (line 104)
   - In `GetWebAdminURL()`, remove the special-case block (lines 435-437):
     ```go
     if name == "cloudbeaver" {
         return a.CloudBeaver.URL()
     }
     ```
   - In `OpenWebAdmin()`, remove the CloudBeaver special-case block (lines 455-458):
     ```go
     // CloudBeaver is a regular Service but has a browser-accessible URL.
     if name == "cloudbeaver" {
         return browser.OpenURL(a.CloudBeaver.URL())
     }
     ```
   - In `InstallVersion()`, update the doc comment on line 381: remove "cloudbeaver" from the parenthetical list. Change `"Works with both versioned services (php, nodejs) and regular services (mysql, postgresql, cloudbeaver)."` to `"Works with both versioned services (php, nodejs) and regular services (mysql, postgresql)."`

3. Edit `internal/config/defaults.go`:
   - Remove the `DefaultCloudBeaverPort` constant (lines 29-30):
     ```go
     // DefaultCloudBeaverPort is the port for the standalone CloudBeaver server.
     DefaultCloudBeaverPort = 8978
     ```

4. Edit `internal/services/service.go`:
   - In the package doc comment, remove `CloudBeaver  -> Service` from the interface assignment list (line 13).
   - In the `Service` interface doc comment (line 30), remove "CloudBeaver" from the parenthetical: change `"(Nginx, DNSMasq, MySQL, PostgreSQL, CloudBeaver)"` to `"(Nginx, DNSMasq, MySQL, PostgreSQL)"`.

5. Note: D-03 says to remove `internal/services/pgadmin/` but that directory does not exist (already cleaned up in v1.0). No action needed for D-03.
  </action>
  <acceptance_criteria>
    - Directory `internal/services/cloudbeaver/` does not exist
    - `app.go` does not contain the string `cloudbeaver` (case-insensitive)
    - `app.go` does not contain the string `CloudBeaver` 
    - `internal/config/defaults.go` does not contain `DefaultCloudBeaverPort`
    - `internal/config/defaults.go` does not contain `8978`
    - `internal/services/service.go` does not contain `CloudBeaver`
    - `go build ./...` succeeds with exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rizkirmdhn/Documents/Code/lamboserver && test ! -d internal/services/cloudbeaver && ! grep -qi cloudbeaver app.go && ! grep -q DefaultCloudBeaverPort internal/config/defaults.go && ! grep -q CloudBeaver internal/services/service.go && go build ./... && echo "PASS"</automated>
  </verify>
  <done>The entire CloudBeaver Go package is deleted. app.go has no CloudBeaver import, field, registration, or special-case URL handling. DefaultCloudBeaverPort is removed from config. Service interface docs no longer mention CloudBeaver. The project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Remove CloudBeaver from frontend and docs</name>
  <files>
    frontend/src/pages/PgDatabasePage.tsx,
    docs/DEVELOPMENT.md
  </files>
  <read_first>
    frontend/src/pages/PgDatabasePage.tsx,
    docs/DEVELOPMENT.md,
    .planning/phases/05-remove-cloudbeaver/05-UI-SPEC.md
  </read_first>
  <action>
1. Edit `frontend/src/pages/PgDatabasePage.tsx` per the UI-SPEC Removed Elements Checklist:
   - Remove state declarations (lines 34-37):
     ```tsx
     const [cbInstalled, setCbInstalled] = useState(false);
     const [cbRunning, setCbRunning] = useState(false);
     const [cbLoading, setCbLoading] = useState("");
     const [cbError, setCbError] = useState<string | null>(null);
     ```
   - In `loadStatus()`, remove CloudBeaver status fetch (lines 52-54):
     ```tsx
     const cbStatus = statuses["cloudbeaver"] || "not_installed";
     setCbRunning(cbStatus === "running");
     setCbInstalled(cbStatus !== "not_installed");
     ```
   - Remove the entire `handleCbAction` function (lines 168-192).
   - Remove the entire CloudBeaver card section from JSX (lines 356-415, the `{/* CloudBeaver */}` comment through the closing `</div>`).
   - Remove unused imports from the Wails import block:
     - Remove `OpenWebAdmin` from the import if no other code uses it (only CloudBeaver used it)
     - Remove `GetWebAdminURL` from the import if no other code uses it (only CloudBeaver used it)
   - Remove unused icon imports from lucide-react:
     - Remove `ExternalLink` (only used in CB "Open" button)
     - Remove `Play` (only used in CB "Start" button)
     - Remove `Square` (only used in CB "Stop" button)
     - Remove `Download` (only used in CB "Install" button)
     - Keep: `Database`, `Trash2`, `Plus`, `RefreshCw` (used by PostgreSQL controls)
   - The final import line should be: `import { Database, Trash2, Plus, RefreshCw } from "lucide-react";`
   - The final Wails import should NOT include `OpenWebAdmin` or `GetWebAdminURL` (remove them):
     ```tsx
     import {
       StartService,
       StopService,
       GetAllStatuses,
       InstallVersion,
       InitService,
       ListServiceDatabases,
       CreateServiceDatabase,
       DropServiceDatabase,
     } from "../../wailsjs/go/main/App";
     ```

2. Edit `docs/DEVELOPMENT.md`:
   - Remove the cloudbeaver directory entry from the project structure tree. The lines to remove are:
     ```
     │   │   └── cloudbeaver/ # CloudBeaver web admin (PostgreSQL)
     │   │       ├── cloudbeaver.go       # Manager: Install, Start, Stop, Status
     ```
     (and any additional lines in that tree entry for cloudbeaver files)
  </action>
  <acceptance_criteria>
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain the string `cloudbeaver` (case-insensitive)
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `cbInstalled`
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `cbRunning`
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `cbLoading`
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `cbError`
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `handleCbAction`
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `OpenWebAdmin`
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `GetWebAdminURL`
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `ExternalLink`
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `Play,` or `Play }` (the icon import)
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `Square`
    - `frontend/src/pages/PgDatabasePage.tsx` does not contain `Download`
    - `frontend/src/pages/PgDatabasePage.tsx` contains `Database` and `Trash2` and `Plus` and `RefreshCw` (PostgreSQL icons retained)
    - `docs/DEVELOPMENT.md` does not contain `cloudbeaver` (case-insensitive)
    - `grep -ri cloudbeaver app.go internal/ frontend/src/ docs/` returns no results
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rizkirmdhn/Documents/Code/lamboserver && ! grep -qi cloudbeaver frontend/src/pages/PgDatabasePage.tsx && ! grep -q cbInstalled frontend/src/pages/PgDatabasePage.tsx && ! grep -q handleCbAction frontend/src/pages/PgDatabasePage.tsx && ! grep -qi cloudbeaver docs/DEVELOPMENT.md && ! grep -ri cloudbeaver app.go internal/ frontend/src/ docs/ && echo "PASS"</automated>
  </verify>
  <done>PgDatabasePage.tsx shows only PostgreSQL service controls with no CloudBeaver panel, state, or functions. All CloudBeaver-only icon and Wails imports are removed. docs/DEVELOPMENT.md has no CloudBeaver references. A codebase-wide grep for "cloudbeaver" in source files returns zero results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced -- this is a pure removal phase.

| Boundary | Description |
|----------|-------------|
| N/A | No new code, no new inputs, no new endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Removal completeness | accept | Verification grep confirms zero "cloudbeaver" references; incomplete removal would cause compile errors |
</threat_model>

<verification>
1. `go build ./...` compiles successfully
2. `grep -ri cloudbeaver app.go internal/ frontend/src/ docs/` returns zero results
3. `test ! -d internal/services/cloudbeaver` confirms directory deleted
4. `test ! -d internal/services/pgadmin` confirms pgadmin already absent (D-03 pre-satisfied)
5. PgDatabasePage.tsx retains all PostgreSQL functionality (Database, Trash2, Plus, RefreshCw icons; StartService, StopService imports; all PostgreSQL state and handlers)
</verification>

<success_criteria>
- The Go project compiles with zero CloudBeaver references
- A codebase-wide search for "cloudbeaver" (case-insensitive) in Go, TSX, and docs/ MD files returns zero results
- The PostgreSQL database page renders only PostgreSQL service controls (start/stop/status, create/drop database)
- No regressions in PostgreSQL page functionality (all existing state, handlers, and UI elements for PostgreSQL are preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/05-remove-cloudbeaver/05-01-SUMMARY.md`
</output>
