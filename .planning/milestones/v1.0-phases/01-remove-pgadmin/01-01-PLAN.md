---
phase: 01-remove-pgadmin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/services/pgadmin/manager.go
  - internal/services/pgadmin/service_adapter.go
  - internal/services/pgadmin/interfaces.go
  - internal/services/pgadmin/manager_test.go
  - app.go
  - internal/system/paths.go
  - internal/system/paths_test.go
autonomous: true
requirements: [REM-01, REM-02, REM-03, REM-04, REM-05]

must_haves:
  truths:
    - "internal/services/pgadmin/ directory does not exist"
    - "app.go has zero references to pgadmin"
    - "paths.go has zero PgAdmin methods"
    - "paths_test.go has zero PgAdmin assertions"
    - "Go code compiles cleanly (go build ./...)"
    - "All Go tests pass (go test ./...)"
  artifacts:
    - path: "app.go"
      provides: "Composition root without pgAdmin"
      contains: "no pgadmin import, no PgAdmin field, no OpenPgAdmin method, no pgadmin registration"
    - path: "internal/system/paths.go"
      provides: "Path helpers without pgAdmin"
      contains: "no PgAdminAppPath, no PgAdminSupportDir, no PgAdminServersFile"
    - path: "internal/system/paths_test.go"
      provides: "Path tests without pgAdmin assertions"
      contains: "no PgAdminAppPath assertion"
  key_links:
    - from: "app.go"
      to: "internal/services/manager.go"
      via: "service registration"
      pattern: "mgr\\.RegisterWebAdmin.*pgadmin"
---

<objective>
Delete all pgAdmin Go code and strip every reference from the backend so the project compiles cleanly.

Purpose: pgAdmin is being replaced by CloudBeaver. This plan removes the entire pgAdmin package and all references from the composition root and system paths, satisfying REM-01 through REM-05.

Output: A codebase with zero pgAdmin Go code that passes `go build ./...` and `go test ./...`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-remove-pgadmin/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete pgAdmin package and strip app.go references</name>
  <files>internal/services/pgadmin/ (delete entire directory), app.go</files>
  <read_first>
    - app.go (current state with all pgAdmin references)
    - internal/services/pgadmin/manager.go (confirm package structure before deletion)
    - internal/services/pgadmin/service_adapter.go (confirm adapter pattern)
  </read_first>
  <action>
1. Delete the entire `internal/services/pgadmin/` directory (all 4 files: manager.go, service_adapter.go, interfaces.go, manager_test.go). Per REM-01.

2. Edit `app.go` to remove ALL pgAdmin references. Specifically:
   - **Remove import** (line 17): Delete `"github.com/rizkirmdhnnn/lamboserver/internal/services/pgadmin"`. Per REM-02.
   - **Remove struct field** (line 51): Delete `PgAdmin *pgadmin.Manager` from the App struct. Per REM-02.
   - **Remove manager creation** (line 77): Delete `pgAdminMgr := pgadmin.NewManager(paths, nil, nil)`. Per REM-02.
   - **Remove WebAdmin registration** (line 91): Delete `mgr.RegisterWebAdmin("pgadmin", pgadmin.NewServiceAdapter(pgAdminMgr))`. Per REM-03.
   - **Remove struct initialization** (line 108): Delete `PgAdmin: pgAdminMgr,` from the return block. Per REM-02.
   - **Remove entire OpenPgAdmin method** (lines 481-489): Delete the `func (a *App) OpenPgAdmin() error` method and its doc comment. Per REM-05.

3. After editing, ensure no blank line gaps are left where code was removed (clean up consecutive blank lines to single blank lines).
  </action>
  <verify>
    <automated>cd /Users/rizkirmdhn/Documents/Code/lamboserver && ! test -d internal/services/pgadmin && ! grep -q 'pgadmin\|PgAdmin\|pgAdmin' app.go && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/services/pgadmin/` directory does not exist (verified by `! test -d internal/services/pgadmin`)
    - `app.go` does not contain the string `pgadmin` (case-insensitive grep returns no matches)
    - `app.go` does not contain `PgAdmin` (no struct field, no method, no variable)
    - `app.go` does not contain `OpenPgAdmin`
    - `app.go` does not contain `RegisterWebAdmin("pgadmin"`
    - `go build ./...` exits with code 0
  </acceptance_criteria>
  <done>pgAdmin package deleted, all app.go references removed, Go code compiles</done>
</task>

<task type="auto">
  <name>Task 2: Remove pgAdmin path helpers and tests</name>
  <files>internal/system/paths.go, internal/system/paths_test.go</files>
  <read_first>
    - internal/system/paths.go (current state with PgAdmin path methods at lines 194-204)
    - internal/system/paths_test.go (current state with PgAdmin assertions at line 203)
  </read_first>
  <action>
1. Edit `internal/system/paths.go` to remove these three methods and their doc comments. Per REM-04:
   - `PgAdminAppPath()` (line 194-195): Returns `/Applications/pgAdmin 4.app`
   - `PgAdminSupportDir()` (lines 197-199): Returns `~/Library/Application Support/pgAdmin`
   - `PgAdminServersFile()` (lines 202-204): Returns `~/Library/Application Support/pgAdmin/servers.json`

2. Edit `internal/system/paths_test.go` to remove the pgAdmin assertion. Per REM-04:
   - In `TestPaths_PostgreSQLPaths` (line 203): Delete the line `assert.Equal(t, "/Applications/pgAdmin 4.app", p.PgAdminAppPath())`
   - Note: The PgAdminSupportDir and PgAdminServersFile methods are not tested in the test file, so only the one assertion needs removal.

3. After editing, ensure no blank line gaps are left where code was removed.
  </action>
  <verify>
    <automated>cd /Users/rizkirmdhn/Documents/Code/lamboserver && ! grep -q 'PgAdmin' internal/system/paths.go && ! grep -q 'PgAdmin' internal/system/paths_test.go && go test ./internal/system/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/system/paths.go` does not contain the string `PgAdmin` (grep returns no matches)
    - `internal/system/paths_test.go` does not contain the string `PgAdmin` (grep returns no matches)
    - `go test ./internal/system/... -count=1` exits with code 0
    - `go test ./... -count=1` exits with code 0 (full test suite per D-02)
  </acceptance_criteria>
  <done>All pgAdmin path helpers and test assertions removed, full test suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan only deletes code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | N/A | N/A | accept | Pure deletion phase introduces no new attack surface. Existing CloudBeaver and phpMyAdmin registrations unchanged. |
</threat_model>

<verification>
After both tasks complete:
1. `go build ./...` compiles with zero errors
2. `go test ./... -count=1` passes with zero failures (per D-02)
3. `grep -ri pgadmin app.go internal/system/paths.go internal/system/paths_test.go` returns empty (no matches)
4. `test -d internal/services/pgadmin` returns false (directory gone)
</verification>

<success_criteria>
- The `internal/services/pgadmin/` directory no longer exists
- `app.go` contains zero references to pgadmin (import, field, init, registration, method)
- `paths.go` contains zero PgAdmin methods
- `paths_test.go` contains zero PgAdmin assertions
- `go build ./...` succeeds
- `go test ./...` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-remove-pgadmin/01-01-SUMMARY.md`
</output>
