---
phase: 01-remove-pgadmin
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - frontend/wailsjs/go/main/App.d.ts
  - frontend/wailsjs/go/main/App.js
autonomous: true
requirements: [REM-06]

must_haves:
  truths:
    - "Wails bindings contain zero pgAdmin references"
    - "Frontend TypeScript bindings reflect current Go methods exactly"
    - "Full build and test suite pass after regeneration"
  artifacts:
    - path: "frontend/wailsjs/go/main/App.d.ts"
      provides: "TypeScript type declarations for Go App methods"
      contains: "no OpenPgAdmin declaration"
    - path: "frontend/wailsjs/go/main/App.js"
      provides: "JavaScript bindings for Go App methods"
      contains: "no OpenPgAdmin function"
  key_links:
    - from: "frontend/wailsjs/go/main/App.js"
      to: "app.go"
      via: "Wails code generation"
      pattern: "window\\['go'\\]\\['main'\\]\\['App'\\]"
---

<objective>
Regenerate Wails frontend bindings to remove stale pgAdmin references from auto-generated TypeScript/JavaScript files.

Purpose: After Plan 01 deleted `OpenPgAdmin()` from `app.go`, the auto-generated binding files still reference it. Per D-01, we regenerate using `wails generate module` rather than manually editing. This satisfies REM-06.

Output: Clean Wails bindings with zero pgAdmin references, verified by full build and test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-remove-pgadmin/01-CONTEXT.md
@.planning/phases/01-remove-pgadmin/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate Wails bindings and verify no pgAdmin references remain</name>
  <files>frontend/wailsjs/go/main/App.d.ts, frontend/wailsjs/go/main/App.js</files>
  <read_first>
    - frontend/wailsjs/go/main/App.d.ts (current state showing OpenPgAdmin on line 68)
    - frontend/wailsjs/go/main/App.js (current state showing OpenPgAdmin on lines 121-122)
    - app.go (confirm OpenPgAdmin method is already gone from Plan 01)
  </read_first>
  <action>
1. Run Wails binding regeneration per D-01:
   ```
   cd /Users/rizkirmdhn/Documents/Code/lamboserver && wails generate module
   ```
   This rebuilds `frontend/wailsjs/go/main/App.d.ts` and `frontend/wailsjs/go/main/App.js` from the current Go methods on the App struct.

2. After regeneration, verify the output files no longer contain `OpenPgAdmin` or any other pgAdmin reference.

3. If `wails generate module` fails for any reason, fall back to manual removal:
   - In `App.d.ts`: Delete the line `export function OpenPgAdmin():Promise<void>;`
   - In `App.js`: Delete the lines containing `export function OpenPgAdmin()` and `return window['go']['main']['App']['OpenPgAdmin']();`
   Note: Manual fallback is a last resort; D-01 explicitly prefers regeneration.

4. Run `go build ./...` and `go test ./... -count=1` to confirm the entire project is clean per D-02.
  </action>
  <verify>
    <automated>cd /Users/rizkirmdhn/Documents/Code/lamboserver && ! grep -qi 'pgadmin\|OpenPgAdmin' frontend/wailsjs/go/main/App.d.ts frontend/wailsjs/go/main/App.js && go build ./... && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/wailsjs/go/main/App.d.ts` does not contain `OpenPgAdmin` (grep returns no matches)
    - `frontend/wailsjs/go/main/App.js` does not contain `OpenPgAdmin` (grep returns no matches)
    - Neither file contains `pgadmin` (case-insensitive grep returns no matches)
    - `go build ./...` exits with code 0
    - `go test ./... -count=1` exits with code 0 (full verification per D-02)
  </acceptance_criteria>
  <done>Wails bindings regenerated with zero pgAdmin references, full build and test suite pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Auto-generated bindings are rebuilt from existing trusted Go source.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | N/A | N/A | accept | Wails regeneration produces bindings from trusted Go source code. No new attack surface. |
</threat_model>

<verification>
After task completes:
1. `grep -ri pgadmin frontend/wailsjs/` returns empty (no matches anywhere in wailsjs)
2. `go build ./...` compiles with zero errors
3. `go test ./... -count=1` passes with zero failures
4. Combined with Plan 01 results: all 5 success criteria from ROADMAP Phase 1 are met
</verification>

<success_criteria>
- `frontend/wailsjs/` contains no auto-generated bindings referencing pgAdmin
- Full project builds cleanly
- Full test suite passes
- All Phase 1 success criteria from ROADMAP are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-remove-pgadmin/01-02-SUMMARY.md`
</output>
