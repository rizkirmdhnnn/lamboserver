---
phase: 02-verify-cloudbeaver-and-clean-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEVELOPMENT.md
autonomous: true
requirements:
  - CB-01
  - CB-02
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "CloudBeaver is the sole PostgreSQL admin interface in the UI"
    - "CloudBeaver connection preset matches the PostgreSQL service config (localhost:5432, user postgres, trust auth)"
    - "docs/DEVELOPMENT.md contains no pgAdmin references"
    - "A codebase-wide search for pgadmin (case-insensitive, excluding .planning/ and .claude/) returns zero results"
  artifacts:
    - path: "docs/DEVELOPMENT.md"
      provides: "Updated directory tree with cloudbeaver replacing pgadmin"
      contains: "cloudbeaver/"
  key_links:
    - from: "internal/services/cloudbeaver/config.go"
      to: "internal/services/postgres/manager.go"
      via: "matching host/port/user values"
      pattern: "localhost.*5432.*postgres"
---

<objective>
Verify CloudBeaver is the sole working PostgreSQL admin interface and remove all remaining pgAdmin references from source code and documentation.

Purpose: Complete the pgAdmin-to-CloudBeaver migration by confirming CloudBeaver wiring is correct and cleaning up the last documentation reference.
Output: Updated docs/DEVELOPMENT.md, verified CloudBeaver configuration, clean codebase with zero pgAdmin references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-verify-cloudbeaver-and-clean-up/02-CONTEXT.md
@.planning/phases/02-verify-cloudbeaver-and-clean-up/02-PATTERNS.md

<interfaces>
<!-- CloudBeaver service files (read-only verification targets) -->

From internal/services/cloudbeaver/config.go (lines 17-51):
Connection preset: host=localhost, port=5432, database=postgres, user=postgres

From internal/services/postgres/manager.go (lines 156-169):
PostgreSQL config: listen_addresses='localhost', port=5432

From internal/services/postgres/manager.go (lines 173-183):
pg_hba.conf: trust auth for local, 127.0.0.1/32, ::1/128

From internal/config/defaults.go (lines 22-24):
DefaultPostgresPort = 5432
DefaultCloudBeaverPort = 8978

From app.go (lines 80-87):
mgr.Register("cloudbeaver", cloudbeaver.NewServiceAdapter(cloudbeaverMgr))
mgr.RegisterWebAdmin("phpmyadmin", phpmyadmin.NewServiceAdapter(phpMyAdminMgr))

From app.go (lines 455-459):
if name == "cloudbeaver" { return browser.OpenURL(a.CloudBeaver.URL()) }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CloudBeaver wiring and connection presets</name>
  <files>none (read-only verification)</files>
  <read_first>
    - internal/services/cloudbeaver/config.go
    - internal/services/cloudbeaver/cloudbeaver.go
    - internal/services/cloudbeaver/service_adapter.go
    - internal/services/postgres/manager.go
    - internal/config/defaults.go
    - frontend/src/pages/PgDatabasePage.tsx
    - app.go
  </read_first>
  <action>
Verify all four conditions by reading source files:

1. CB-01 — CloudBeaver is sole PostgreSQL admin interface:
   - Read `frontend/src/pages/PgDatabasePage.tsx` and confirm it renders ONLY a CloudBeaver panel. There must be zero imports, components, or references to pgAdmin.
   - Read `app.go` and confirm CloudBeaver is registered via `mgr.Register("cloudbeaver", ...)` and the `OpenWebAdmin` method handles cloudbeaver via `browser.OpenURL(a.CloudBeaver.URL())`.
   - Read `internal/services/cloudbeaver/service_adapter.go` and confirm the compile-time check: `var _ services.Service = (*ServiceAdapter)(nil)`.

2. CB-02 — Connection preset matches PostgreSQL config:
   - Read `internal/services/cloudbeaver/config.go` and extract the preset values: host, port, database, user.
   - Read `internal/services/postgres/manager.go` writeConf() and confirm: `listen_addresses = 'localhost'`, `port = 5432`.
   - Read `internal/services/postgres/manager.go` writeHba() and confirm trust auth for local connections.
   - Read `internal/config/defaults.go` and confirm `DefaultPostgresPort = 5432`.
   - Cross-check: CloudBeaver preset (localhost:5432, user postgres) must match PostgreSQL config (localhost:5432, trust auth for local).

3. DOC-02 — No remaining pgAdmin references in source/docs scope:
   - Run: `grep -ri "pgadmin" --include="*.go" --include="*.ts" --include="*.tsx" --include="*.js" --include="*.jsx" --include="*.md" --exclude-dir=".planning" --exclude-dir=".claude" --exclude-dir="node_modules" --exclude-dir=".git" .`
   - Expected: Only `docs/DEVELOPMENT.md` (which Task 2 will fix). If any OTHER file matches, fix it in Task 2.

If any verification fails (unexpected pgAdmin references, mismatched config), report the finding so Task 2 can address it.
  </action>
  <verify>
    <automated>cd /Users/rizkirmdhn/Documents/Code/lamboserver && grep -ri "pgadmin" --include="*.go" --include="*.ts" --include="*.tsx" --include="*.js" --include="*.jsx" --include="*.md" --exclude-dir=".planning" --exclude-dir=".claude" --exclude-dir="node_modules" --exclude-dir=".git" . | grep -v "docs/DEVELOPMENT.md" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `internal/services/cloudbeaver/config.go` contains `"host": "localhost"` and `"port": "5432"` and `"user": "postgres"`
    - `internal/services/postgres/manager.go` contains `listen_addresses = 'localhost'` and `port = 5432`
    - `internal/services/cloudbeaver/service_adapter.go` contains `var _ services.Service = (*ServiceAdapter)(nil)`
    - `app.go` contains `mgr.Register("cloudbeaver"` 
    - `app.go` contains `if name == "cloudbeaver"`
    - `frontend/src/pages/PgDatabasePage.tsx` does NOT contain `pgadmin` or `pgAdmin` (case-insensitive)
    - grep for pgadmin in source/docs scope (excluding .planning/ .claude/) returns ONLY docs/DEVELOPMENT.md
  </acceptance_criteria>
  <done>All four requirements (CB-01, CB-02, DOC-02 pre-check) verified through source code review. CloudBeaver preset matches PostgreSQL config. No unexpected pgAdmin references found outside docs/DEVELOPMENT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/DEVELOPMENT.md and run build verification</name>
  <files>docs/DEVELOPMENT.md</files>
  <read_first>
    - docs/DEVELOPMENT.md
  </read_first>
  <action>
1. DOC-01 — Edit `docs/DEVELOPMENT.md` to replace the pgAdmin directory tree entry (lines 127-130) with CloudBeaver.

   DELETE these lines:
   ```
   │   │   └── pgadmin/     # pgAdmin web admin
   │   │       ├── manager.go           # Status, Open
   │   │       ├── service_adapter.go   # Adapts Manager to services.WebAdminService
   │   │       └── interfaces.go        # Interfaces
   ```

   REPLACE with:
   ```
   │   │   └── cloudbeaver/ # CloudBeaver web database manager
   │   │       ├── cloudbeaver.go       # Manager: install, start, stop, URL
   │   │       ├── config.go            # Generates cloudbeaver.conf with PostgreSQL preset
   │   │       └── service_adapter.go   # Adapts Manager to services.Service
   ```

   IMPORTANT: The `phpmyadmin/` entry on line 122 keeps its `├──` connector (it is NOT the last entry since cloudbeaver follows it). The `cloudbeaver/` entry uses `└──` as the last entry under `services/`.

2. Build verification (per D-03):
   - Run `go build ./...` from project root — must exit 0 with no errors
   - Run `cd frontend && npm run build` — must exit 0 with no errors

3. Final DOC-02 confirmation:
   - Run the pgadmin grep again to confirm zero results in source/docs scope (excluding .planning/ and .claude/)
  </action>
  <verify>
    <automated>cd /Users/rizkirmdhn/Documents/Code/lamboserver && grep -c "cloudbeaver/" docs/DEVELOPMENT.md && grep -ci "pgadmin" docs/DEVELOPMENT.md | grep "^0$" && go build ./... && cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `docs/DEVELOPMENT.md` contains `cloudbeaver/ # CloudBeaver web database manager`
    - `docs/DEVELOPMENT.md` contains `cloudbeaver.go` and `config.go` and `service_adapter.go` under the cloudbeaver tree entry
    - `docs/DEVELOPMENT.md` does NOT contain `pgadmin` (case-insensitive grep returns 0 matches)
    - `go build ./...` exits with code 0
    - `cd frontend && npm run build` exits with code 0
    - `grep -ri "pgadmin" --include="*.go" --include="*.ts" --include="*.tsx" --include="*.md" --exclude-dir=".planning" --exclude-dir=".claude" --exclude-dir="node_modules" --exclude-dir=".git" .` returns zero results
  </acceptance_criteria>
  <done>docs/DEVELOPMENT.md updated with CloudBeaver entry replacing pgAdmin. Both Go and frontend builds pass. Codebase-wide pgAdmin search returns zero results in source/docs scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries are crossed in this phase. This is a documentation edit and read-only verification of existing code. No new endpoints, inputs, or authentication flows are introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | docs/DEVELOPMENT.md | accept | Documentation update only; no secrets or credentials exposed. Directory tree is public project structure. |
</threat_model>

<verification>
1. `grep -ri "pgadmin" --include="*.go" --include="*.ts" --include="*.tsx" --include="*.md" --exclude-dir=".planning" --exclude-dir=".claude" --exclude-dir="node_modules" --exclude-dir=".git" .` returns zero results
2. `docs/DEVELOPMENT.md` contains `cloudbeaver/` entry and no `pgadmin` entry
3. `go build ./...` exits 0
4. `cd frontend && npm run build` exits 0
5. CloudBeaver config preset matches PostgreSQL service config (localhost:5432, user postgres)
</verification>

<success_criteria>
- CloudBeaver verified as sole PostgreSQL admin interface (CB-01)
- CloudBeaver connection presets confirmed matching PostgreSQL config (CB-02)
- docs/DEVELOPMENT.md updated with CloudBeaver, no pgAdmin references (DOC-01)
- Zero pgAdmin references in source/docs scope excluding .planning/ and .claude/ (DOC-02)
- Both Go backend and frontend build successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-verify-cloudbeaver-and-clean-up/02-01-SUMMARY.md`
</output>
