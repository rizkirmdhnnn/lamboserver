---
phase: 07-app-bundle-icon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wails.json
  - build/darwin/Info.plist
  - build/darwin/Info.dev.plist
autonomous: true
requirements:
  - BUNDLE-01
  - BUNDLE-02
  - BUNDLE-03

must_haves:
  truths:
    - "wails.json contains an info block with productName, productVersion, copyright, and comments"
    - "Both Info.plist and Info.dev.plist use dev.lamboserver.app as CFBundleIdentifier (not com.wails.*)"
    - "Info.plist template variables reference wails.json info fields correctly"
  artifacts:
    - path: "wails.json"
      provides: "App metadata source of truth"
      contains: "productName"
    - path: "build/darwin/Info.plist"
      provides: "Production plist template with correct bundle ID"
      contains: "dev.lamboserver.app"
    - path: "build/darwin/Info.dev.plist"
      provides: "Dev plist template with correct bundle ID"
      contains: "dev.lamboserver.app"
  key_links:
    - from: "wails.json info block"
      to: "build/darwin/Info.plist"
      via: "Go template rendering at wails build time"
      pattern: "Info\\.ProductName|Info\\.ProductVersion|Info\\.Copyright|Info\\.Comments"
---

<objective>
Add the wails.json info block with app metadata and fix CFBundleIdentifier in both plist templates.

Purpose: LamboServer's .app bundle must display correct name, version, copyright, and bundle identifier instead of Wails placeholder defaults. This metadata is required before production bundling in Phase 8.

Output: Updated wails.json with info block, both plist files with literal bundle identifier dev.lamboserver.app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-app-bundle-icon/07-CONTEXT.md
@.planning/phases/07-app-bundle-icon/07-RESEARCH.md
@.planning/phases/07-app-bundle-icon/07-PATTERNS.md

<interfaces>
<!-- Current wails.json (lines 1-13) — no info block exists yet -->
From wails.json:
```json
{
  "$schema": "https://wails.io/schemas/config.v2.json",
  "name": "LamboServer",
  "outputfilename": "LamboServer",
  "frontend:install": "npm install",
  "frontend:build": "npm run build",
  "frontend:dev:watcher": "npm run dev",
  "frontend:dev:serverUrl": "auto",
  "author": {
    "name": "Achmad Rizki Ramadhan",
    "email": "achmadrizkiramadhan0101@gmail.com"
  }
}
```

From build/darwin/Info.plist (line 10-11 — the key to change):
```xml
<key>CFBundleIdentifier</key>
<string>com.wails.{{.Name}}</string>
```

From build/darwin/Info.dev.plist (line 10-11 — identical placeholder):
```xml
<key>CFBundleIdentifier</key>
<string>com.wails.{{.Name}}</string>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add info block to wails.json</name>
  <files>wails.json</files>
  <read_first>wails.json</read_first>
  <action>
Add the `info` block to `wails.json` after the existing `author` block. The final file must be valid JSON with this exact structure:

```json
{
  "$schema": "https://wails.io/schemas/config.v2.json",
  "name": "LamboServer",
  "outputfilename": "LamboServer",
  "frontend:install": "npm install",
  "frontend:build": "npm run build",
  "frontend:dev:watcher": "npm run dev",
  "frontend:dev:serverUrl": "auto",
  "author": {
    "name": "Achmad Rizki Ramadhan",
    "email": "achmadrizkiramadhan0101@gmail.com"
  },
  "info": {
    "companyName": "LamboServer",
    "productName": "LamboServer",
    "productVersion": "1.0.0",
    "copyright": "\u00a9 2026 LamboServer",
    "comments": "Local web development environment manager for macOS"
  }
}
```

Per D-01: bundle identifier is handled in the plist files, not here.
Per D-03: productName is "LamboServer".
Per D-07: productVersion is "1.0.0".
Per D-02: copyright is "(c) 2026 LamboServer" — use the Unicode escape `\u00a9` for the copyright symbol to avoid UTF-8 encoding issues.
Per D-08: productVersion here templates into CFBundleVersion and CFBundleShortVersionString in Info.plist.
Claude's discretion: comments field set to "Local web development environment manager for macOS".
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('wails.json')); assert d['info']['productName']=='LamboServer'; assert d['info']['productVersion']=='1.0.0'; assert '2026' in d['info']['copyright']; assert d['info']['comments']!=''; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; d=json.load(open('wails.json')); print(d['info']['productName'])"` outputs `LamboServer`
    - `python3 -c "import json; d=json.load(open('wails.json')); print(d['info']['productVersion'])"` outputs `1.0.0`
    - `python3 -c "import json; d=json.load(open('wails.json')); print(d['info']['copyright'])"` contains `2026 LamboServer`
    - `python3 -c "import json; d=json.load(open('wails.json')); print(d['info']['companyName'])"` outputs `LamboServer`
    - `python3 -c "import json; d=json.load(open('wails.json')); print(d['info']['comments'])"` is not empty
    - File is valid JSON: `python3 -c "import json; json.load(open('wails.json')); print('VALID')"` outputs `VALID`
  </acceptance_criteria>
  <done>wails.json contains a complete info block with all 5 fields (companyName, productName, productVersion, copyright, comments) and file is valid JSON</done>
</task>

<task type="auto">
  <name>Task 2: Fix CFBundleIdentifier in both plist templates</name>
  <files>build/darwin/Info.plist, build/darwin/Info.dev.plist</files>
  <read_first>build/darwin/Info.plist, build/darwin/Info.dev.plist</read_first>
  <action>
In BOTH `build/darwin/Info.plist` and `build/darwin/Info.dev.plist`, replace line 11:

```xml
<string>com.wails.{{.Name}}</string>
```

with the literal string:

```xml
<string>dev.lamboserver.app</string>
```

Per D-01: The bundle identifier must be `dev.lamboserver.app` (product-focused namespace).
Per RESEARCH Pattern 2: CFBundleIdentifier is NOT a Wails template variable — it must be a literal string. Do NOT use `{{.Info.BundleIdentifier}}` (that variable does not exist in Wails v2).

CRITICAL: Only change line 11 (the CFBundleIdentifier value string). All other lines, including all Go template variables (`{{.Info.ProductName}}`, `{{.Info.ProductVersion}}`, etc.), must remain exactly as they are.

CRITICAL: In `Info.dev.plist`, the `NSAppTransportSecurity` block at lines 62-66 must be preserved unchanged.

No other modifications to either file.
  </action>
  <verify>
    <automated>grep -c 'com.wails' build/darwin/Info.plist build/darwin/Info.dev.plist; grep -c 'dev.lamboserver.app' build/darwin/Info.plist build/darwin/Info.dev.plist</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'com.wails.{{.Name}}' build/darwin/Info.plist` returns NO matches (exit code 1)
    - `grep 'com.wails.{{.Name}}' build/darwin/Info.dev.plist` returns NO matches (exit code 1)
    - `grep 'dev.lamboserver.app' build/darwin/Info.plist` returns exactly 1 match
    - `grep 'dev.lamboserver.app' build/darwin/Info.dev.plist` returns exactly 1 match
    - `grep '{{.Info.ProductName}}' build/darwin/Info.plist` still returns a match (template variables preserved)
    - `grep '{{.Info.ProductVersion}}' build/darwin/Info.plist` still returns a match (template variables preserved)
    - `grep 'NSAppTransportSecurity' build/darwin/Info.dev.plist` still returns a match (dev-only block preserved)
  </acceptance_criteria>
  <done>Both plist files have CFBundleIdentifier set to literal "dev.lamboserver.app", all Go template variables intact, Info.dev.plist NSAppTransportSecurity block preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build config -> .app bundle | wails.json and plist templates are consumed by wails build to produce the .app; incorrect values propagate to the shipped artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | CFBundleIdentifier | mitigate | Set to `dev.lamboserver.app` (unique reverse-DNS); prevents collision with other apps on user's system |
| T-07-02 | Tampering | wails.json | accept | Build config is developer-controlled source file; no runtime exposure. Standard git-tracked file. |
| T-07-03 | Information Disclosure | copyright field | accept | Copyright string is intentionally public metadata |
</threat_model>

<verification>
After both tasks complete, verify the three config files are consistent:

1. `python3 -c "import json; d=json.load(open('wails.json')); assert 'info' in d; assert d['info']['productName']=='LamboServer'; assert d['info']['productVersion']=='1.0.0'; print('wails.json OK')"` -- outputs "wails.json OK"
2. `grep -c 'dev.lamboserver.app' build/darwin/Info.plist` -- outputs "1"
3. `grep -c 'dev.lamboserver.app' build/darwin/Info.dev.plist` -- outputs "1"
4. `grep -c 'com.wails.{{.Name}}' build/darwin/Info.plist build/darwin/Info.dev.plist` -- outputs "0" for both files
</verification>

<success_criteria>
- wails.json has info block with productName=LamboServer, productVersion=1.0.0, copyright containing 2026 LamboServer
- Both plist files use dev.lamboserver.app as CFBundleIdentifier
- No Wails placeholder bundle ID (com.wails.*) remains in either plist file
- All Go template variables in plist files are preserved intact
</success_criteria>

<output>
After completion, create `.planning/phases/07-app-bundle-icon/07-01-SUMMARY.md`
</output>
