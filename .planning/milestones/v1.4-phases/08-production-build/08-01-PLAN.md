---
phase: 08-production-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build/darwin/entitlements.plist
  - build/darwin/Info.plist
  - build/darwin/Info.dev.plist
autonomous: true
requirements:
  - BUILD-03

must_haves:
  truths:
    - "entitlements.plist exists with correct entitlement keys for Phase 9 signing"
    - "LSMinimumSystemVersion is 12.0.0 in both plist templates"
    - "com.apple.security.app-sandbox is NOT present in entitlements.plist"
  artifacts:
    - path: "build/darwin/entitlements.plist"
      provides: "Entitlements for ad-hoc codesigning in Phase 9"
      contains: "com.apple.security.cs.allow-jit"
    - path: "build/darwin/Info.plist"
      provides: "Production plist template with updated minimum OS"
      contains: "12.0.0"
    - path: "build/darwin/Info.dev.plist"
      provides: "Dev plist template with updated minimum OS"
      contains: "12.0.0"
  key_links:
    - from: "build/darwin/entitlements.plist"
      to: "codesign (Phase 9)"
      via: "codesign --entitlements flag"
      pattern: "com.apple.security"
---

<objective>
Create build/darwin/entitlements.plist for Phase 9 signing and update LSMinimumSystemVersion from 10.13.0 to 12.0.0 in both plist templates.

Purpose: Prepares the build configuration files so that (a) Phase 9 can immediately codesign the .app without missing entitlements, and (b) the minimum OS version reflects the real requirement (Wails v2 WKWebView on Apple Silicon needs macOS 12+).

Output: Three files modified/created in build/darwin/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-production-build/08-CONTEXT.md
@.planning/phases/08-production-build/08-RESEARCH.md
@.planning/phases/08-production-build/08-PATTERNS.md

<interfaces>
<!-- Key files the executor needs to know about -->

From build/darwin/Info.plist (line 20-21 — the value to change):
```xml
<key>LSMinimumSystemVersion</key>
<string>10.13.0</string>
```

From build/darwin/Info.dev.plist (line 20-21 — identical twin, same change):
```xml
<key>LSMinimumSystemVersion</key>
<string>10.13.0</string>
```

Info.dev.plist has one extra block at lines 62-66 (NSAppTransportSecurity) that MUST be preserved.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create build/darwin/entitlements.plist</name>
  <files>build/darwin/entitlements.plist</files>
  <read_first>
    - build/darwin/Info.plist (analog — same plist XML format, same directory)
    - .planning/phases/08-production-build/08-RESEARCH.md (Code Examples section has the exact file content)
  </read_first>
  <action>
Create a new file `build/darwin/entitlements.plist` with the following exact content:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required: WKWebView uses JIT on Apple Silicon -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Required: Wails WebView needs unsigned memory for JS execution -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Required: Wails embeds third-party frameworks without Apple signatures -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Required: osascript admin dialogs (RunWithAdminPrivileges) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
    <key>NSAppleEventsUsageDescription</key>
    <string>LamboServer uses administrator dialogs to install system services.</string>
    <!-- Required: outgoing HTTP connections (Wails WebView, service health checks) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Required: local service processes bind ports (pgweb, phpMyAdmin) -->
    <key>com.apple.security.network.server</key>
    <true/>
    <!-- NEVER add: com.apple.security.app-sandbox
         This would kill all exec.Command calls to service binaries -->
</dict>
</plist>
```

CRITICAL CONSTRAINT: Do NOT include `com.apple.security.app-sandbox` — it would break all exec.Command calls to service binaries in ~/.lamboserver/. This is a hard architectural constraint.

This file is not consumed by `wails build` (it ignores it). It exists for Phase 9's `codesign --entitlements` invocation.
  </action>
  <verify>
    <automated>test -f build/darwin/entitlements.plist && grep -q "com.apple.security.cs.allow-jit" build/darwin/entitlements.plist && grep -q "com.apple.security.network.client" build/darwin/entitlements.plist && grep -q "com.apple.security.network.server" build/darwin/entitlements.plist && grep -q "NSAppleEventsUsageDescription" build/darwin/entitlements.plist && ! grep -q "com.apple.security.app-sandbox" build/darwin/entitlements.plist && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - build/darwin/entitlements.plist exists
    - File contains `com.apple.security.cs.allow-jit`
    - File contains `com.apple.security.cs.allow-unsigned-executable-memory`
    - File contains `com.apple.security.cs.disable-library-validation`
    - File contains `com.apple.security.automation.apple-events`
    - File contains `NSAppleEventsUsageDescription`
    - File contains `com.apple.security.network.client`
    - File contains `com.apple.security.network.server`
    - File does NOT contain `com.apple.security.app-sandbox`
  </acceptance_criteria>
  <done>entitlements.plist exists in build/darwin/ with all 6 entitlement keys and the NSAppleEventsUsageDescription string, and does NOT contain app-sandbox</done>
</task>

<task type="auto">
  <name>Task 2: Update LSMinimumSystemVersion in both plist templates</name>
  <files>build/darwin/Info.plist, build/darwin/Info.dev.plist</files>
  <read_first>
    - build/darwin/Info.plist (current state — line 21 has 10.13.0)
    - build/darwin/Info.dev.plist (twin file — line 21 has 10.13.0, lines 62-66 NSAppTransportSecurity must be preserved)
  </read_first>
  <action>
In both `build/darwin/Info.plist` and `build/darwin/Info.dev.plist`, change line 21 from:

```xml
<string>10.13.0</string>
```

to:

```xml
<string>12.0.0</string>
```

This is the only change in each file. All other content must remain exactly as-is:
- `CFBundleIdentifier` remains `dev.lamboserver.app` (literal string, set in Phase 7)
- All Go template variables (`{{.Info.ProductName}}`, `{{.Info.ProductVersion}}`, `{{.Info.Copyright}}`, etc.) remain unchanged
- `Info.dev.plist` lines 62-66 (`NSAppTransportSecurity` block with `NSAllowsLocalNetworking`) must be preserved

Rationale: Wails v2 WKWebView on Apple Silicon practically requires macOS 12.0 (Monterey). The default 10.13.0 (High Sierra, 2017) is outdated for this project's target audience.
  </action>
  <verify>
    <automated>grep -c "12.0.0" build/darwin/Info.plist | grep -q "1" && grep -c "12.0.0" build/darwin/Info.dev.plist | grep -q "1" && ! grep -q "10.13.0" build/darwin/Info.plist && ! grep -q "10.13.0" build/darwin/Info.dev.plist && grep -q "dev.lamboserver.app" build/darwin/Info.plist && grep -q "NSAllowsLocalNetworking" build/darwin/Info.dev.plist && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - build/darwin/Info.plist contains `12.0.0` (exactly 1 occurrence)
    - build/darwin/Info.plist does NOT contain `10.13.0`
    - build/darwin/Info.dev.plist contains `12.0.0` (exactly 1 occurrence)
    - build/darwin/Info.dev.plist does NOT contain `10.13.0`
    - build/darwin/Info.plist still contains `dev.lamboserver.app`
    - build/darwin/Info.dev.plist still contains `NSAllowsLocalNetworking`
    - All Go template variables ({{.Info.ProductName}}, {{.Info.ProductVersion}}) remain intact in both files
  </acceptance_criteria>
  <done>Both plist templates show LSMinimumSystemVersion 12.0.0, all other content unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entitlements.plist -> codesign | Entitlements define what the signed app is allowed to do at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | build/darwin/entitlements.plist | mitigate | Verify com.apple.security.app-sandbox is NOT present — its inclusion would be a different kind of threat (breaking functionality), but its absence means the app runs unsandboxed. Accepted because sandboxing is incompatible with the app's architecture (exec.Command to service binaries). |
| T-08-02 | Tampering | build/darwin/entitlements.plist | accept | File is in version control; any unauthorized change is visible in git diff. Ad-hoc signing (Phase 9) does not provide tamper resistance anyway — that requires a Developer ID certificate (deferred DIST-01). |
</threat_model>

<verification>
1. `test -f build/darwin/entitlements.plist` — file exists
2. `grep -c "com.apple.security" build/darwin/entitlements.plist` — returns 6 (six entitlement keys)
3. `! grep -q "app-sandbox" build/darwin/entitlements.plist` — sandbox NOT present
4. `grep "12.0.0" build/darwin/Info.plist build/darwin/Info.dev.plist` — both files updated
5. `grep "dev.lamboserver.app" build/darwin/Info.plist` — bundle ID preserved
</verification>

<success_criteria>
- entitlements.plist created with all required entitlement keys
- No app-sandbox entitlement present
- LSMinimumSystemVersion updated to 12.0.0 in both plist templates
- All existing plist content (bundle ID, template variables, NSAppTransportSecurity) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/08-production-build/08-01-SUMMARY.md`
</output>
